# AI provider for the precacher: gemini or openrouter
AI_PROVIDER=gemini

# Gemini API Key (used when AI_PROVIDER=gemini)
GEMINI_API_KEY=your_gemini_api_key_here

# OpenRouter API key + model (used when AI_PROVIDER=openrouter)
OPENROUTER_API_KEY=your_openrouter_api_key_here
AI_MODEL=moonshotai/kimi-k2
OPENROUTER_SITE_URL=https://www.boyandascanner.com
OPENROUTER_APP_NAME="Boy & A Scanner Precacher"

# Supabase credentials (same ones used in .env.local)
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# URL of the deployed app so the precacher can call /api/rrdb for bounded RR refreshes
APP_BASE_URL=https://app.boyandascanner.com

# Optional: how many seconds between API calls (default: 5)
DELAY_SECONDS=5

# Optional legacy fallback if HOT_MAX_AGE_HOURS is not set
MAX_AGE_HOURS=24

# Hot ZIPs = recent searches, favorites, community activity
# Warm ZIPs = static seed coverage ZIPs
HOT_MAX_AGE_HOURS=24
WARM_MAX_AGE_HOURS=168

# How many ZIPs to consider per signal source on each run
MAX_SEED_ZIPS=150
MAX_RECENT_SEARCH_ZIPS=125
MAX_FAVORITE_ZIPS=75
MAX_REPORT_ZIPS=75
EXPAND_SEED_ZIPS=1
MAX_SEED_APPEND_PER_RUN=40

# Optional bounded RadioReference assist for weekly refreshes
RR_REFRESH_ENABLED=0
RR_REFRESH_HOT_ONLY=1
RR_USERNAME=your_rr_username_here
RR_PASSWORD=your_rr_password_here

# Weekly Monday morning UTC timers used by setup.sh
CACHE_ON_CALENDAR="Mon *-*-* 08:00:00"
SEO_ON_CALENDAR="Mon *-*-* 08:30:00"

# ── SEO Page Generator ──────────────────────────────────────────────
# GitHub Personal Access Token (needs 'repo' scope)
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# GitHub repo to push SEO pages to (format: username/repo-name)
# Create a separate empty repo for this, e.g. "youruser/scanner-seo-pages"
# Connect it to a new Vercel project pointing at the repo root
GITHUB_REPO=tfs2006/scanner-seo-pages

# Canonical URL of the main app (no trailing slash)
SEO_SITE_URL=https://www.boyandascanner.com
